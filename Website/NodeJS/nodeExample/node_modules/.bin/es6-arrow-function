#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../es6-arrow-function/bin/es6-arrow-function" "$@"
else 
  exec node  "$basedir/../es6-arrow-function/bin/es6-arrow-function" "$@"
fi
